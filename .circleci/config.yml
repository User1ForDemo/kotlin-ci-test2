version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: promeg/linux
    steps:
      - run: echo "Hi I'm on Runners!"
      - run: ./gradlew test